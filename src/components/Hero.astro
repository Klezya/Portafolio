---
const techStack = ['Python', 'Docker', 'AWS', 'REST APIs', 'Ciberseguridad'];
---

<section class="section-separated container flex flex-col lg:flex-row items-center justify-between min-h-[88dvh] py-32 gap-20 max-w-6xl">
  <div class="flex flex-col gap-8 flex-1">
    <div class="flex items-center gap-3">
      <span class="inline-block w-8 h-px" style="background-color: var(--color-accent);"></span>
      <span class="text-sm font-medium tracking-widest uppercase" style="color: var(--color-accent);">
        Ingeniero en Computación & Informática
      </span>
    </div>

    <div class="flex flex-col gap-2">
      <p class="text-lg font-medium" style="color: var(--color-text-muted);">Hola, soy</p>
      <h1 class="text-6xl sm:text-7xl md:text-8xl font-bold leading-none tracking-tight" style="color: var(--color-text);">
        Benjamín<br />
        <span style="color: var(--color-accent);">Núñez.</span>
      </h1>
    </div>

    <p class="text-xl sm:text-2xl leading-relaxed max-w-2xl" style="color: var(--color-text-muted);">
      Backend developer especializado en la construcción de APIs robustas
      con <strong style="color: var(--color-text); font-weight: 600;">Python</strong>,
      <strong style="color: var(--color-text); font-weight: 600;">Docker</strong> y
      <strong style="color: var(--color-text); font-weight: 600;">AWS</strong> —
      actualmente explorando el mundo de la
      <strong style="color: var(--color-text); font-weight: 600;">ciberseguridad</strong> y las
      <strong style="color: var(--color-text); font-weight: 600;">redes</strong>.
    </p>

    <p class="text-sm" style="color: var(--color-text-subtle);">
      Egresado · Universidad Andrés Bello
    </p>

    <div class="flex flex-wrap gap-2">
      {techStack.map((tech) => (
        <span class="badge">{tech}</span>
      ))}
    </div>

    <div class="flex flex-wrap gap-4">
      <a href="#proyectos" class="btn-primary">Ver proyectos</a>
      <a href="#contacto" class="btn-secondary">Contacto</a>
      <a href="/cv.pdf" target="_blank" rel="noopener" class="btn-secondary">
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M14.5 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7.5L14.5 2z"/>
          <polyline points="14 2 14 8 20 8"/>
          <line x1="16" y1="13" x2="8" y2="13"/>
          <line x1="16" y1="17" x2="8" y2="17"/>
          <line x1="10" y1="9" x2="8" y2="9"/>
        </svg>
        Ver CV
      </a>
    </div>
  </div>

  <div class="flex-shrink-0 flex flex-col items-center gap-4">
    <div
      class="relative w-64 h-64 sm:w-80 sm:h-80 rounded-2xl overflow-hidden"
      style="border: 2px solid var(--color-border); box-shadow: var(--shadow-lg);"
    >
      <img
        src="/profile.jpg"
        alt="Fotografía de Benjamín Núñez"
        class="w-full h-full object-cover"
        id="hero-photo"
      />
      <div
        id="hero-placeholder"
        class="absolute inset-0 flex flex-col items-center justify-center gap-3 hidden"
        style="background-color: var(--color-bg-muted);"
      >
        <svg xmlns="http://www.w3.org/2000/svg" width="56" height="56" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round" style="color: var(--color-text-subtle);">
          <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/>
        </svg>
        <span class="text-xs font-medium" style="color: var(--color-text-subtle);">Tu fotografía aquí</span>
      </div>
    </div>

    <div class="flex items-center gap-2">
      <span class="w-2 h-2 rounded-full bg-emerald-400 animate-pulse"></span>
      <span class="text-xs font-medium" style="color: var(--color-text-muted);">Disponible para trabajar</span>
    </div>
  </div>
</section>
